# Dependencies and build output
node_modules/
dist/
*.tsbuildinfo

# Secrets must never be committed (ADR 0007 BYOK). Runtime credentials and the
# session store live under %LOCALAPPDATA%\thcode, outside this tree — this only
# guards a stray local .env.
.env
.env.*

# Logs
*.log
npm-debug.log*
