---
scope: thcode CLI Release 1 — headless agent core + Ink TUI
purpose: Fix the invariants that keep independently-built units (provider adapters, local tools, specialist-service adapters, UI) from diverging; ratify settled ADR/scaffold conventions rather than re-derive them.
altitude: feature (keeps epics)
updated: 2026-07-16T20:51
---

- (direction) Fast path selected: draft the complete spine with explicit [ASSUMPTION] tags where existing reality and inputs do not settle a load-bearing call.
- (direction) Deliverable is the terse architecture spine only; audience is implementers and coding agents building Release 1 epics independently.
- (event) architecture run restarted clean at user request
- (constraint) Release 1 is a Thai-first local CLI harness: Typhoon is the launch reasoning provider, four AI-for-Thai specialist services must be supported, credentials stay local, and macOS plus Windows are launch platforms.
- (constraint) Hosted client/server components are outside the Release 1 request path; the active product implementation is cli/.
- (constraint) Existing adopted boundaries remain binding: Ink dispatches typed intents through CoreApp; remote/model outputs are proposals; local policy exclusively authorizes side effects; Plan mode is structurally read-only; saved transcript and Active Model Context are distinct.
- (decision) [ADOPTED] Use Hexagonal Architecture (Ports-and-Adapters) with one local Policy Enforcement Point. Binds all Release 1 units. Prevents UI, provider, specialist-service, persistence, and OS implementations from becoming competing authorities. Rule: application orchestration depends on ports and domain contracts; concrete adapters depend inward; every side effect crosses the local enforcement point.
- (decision) [ADOPTED] CoreApp is the sole UI-facing application facade. Binds UI and application protocol. Prevents TUI components from owning runtime state or bypassing policy. Rule: UI sends typed intents and subscribes to typed events; it never imports provider, persistence, filesystem, network, process, credential, or specialist-service adapters.
- (decision) [ASSUMPTION] Canonical application protocol uses PromptRoundId, SessionId, OperationId, monotonic Sequence, Timestamp, deterministic/model provenance, and typed payloads. Binds CoreApp, AgentLoop, UI, persistence, and telemetry. Prevents incompatible event streams and uncorrelated evidence. Rule: streaming, tool proposals/results, approvals, specialist calls, interruptions, completion, and failure are ordered events under one prompt round; cancellation is requested through CoreApp and finalized by the application layer.
- (decision) [ADOPTED] Treat every provider or specialist-service response as untrusted data. Binds all remote adapters and tool execution. Prevents remote code or model output from acquiring local authority. Rule: adapters may normalize responses and propose actions only; local schema validation, mode checks, permission checks, consent, workspace/process policy, and audit emission happen before execution.
- (decision) [ASSUMPTION] Define stable ports for ReasoningProvider, SpecialistService, CredentialStore, SessionRepository, CheckpointRepository, ArtifactStore, HealthRegistry, ContextBuilder, and PlatformBoundary. Binds independently-built adapters. Prevents each epic inventing lifecycle, persistence, and failure semantics. Rule: ports exchange canonical domain values and failure envelopes, never vendor SDK objects, database rows, Ink values, or OS-specific process objects.
- (decision) [ASSUMPTION] Session is the single persisted aggregate root for transcript, prompt rounds, runtime mode/profile, active-context decisions, evidence references, token ledger, interruption markers, boundaries, and checkpoint lineage. Binds persistence and restoration. Prevents in-memory runtime and SQLite from becoming dual authorities. Rule: application commands serialize mutations per SessionId through SessionRepository transactions; restoration rehydrates facts but resets execution authority to a non-escalated state.
- (decision) [ADOPTED] Full transcript is immutable local history; Active Model Context is a derived, bounded projection. Binds context and provider dispatch. Prevents lossy persistence or uncontrolled prompt growth. Rule: context construction records inclusion/compaction decisions, protects pinned/current-round content, and fails before provider dispatch when protected context cannot fit.
- (decision) [ASSUMPTION] Remote dependencies share one health lifecycle: unconfigured, checking, available, unavailable, unhealthy, quarantined; only an explicit retest exits quarantine. Binds provider and specialist configuration. Prevents silent fallback and inconsistent retry behavior. Rule: health is keyed by a secret-free configuration fingerprint, persisted locally, and updated only by HealthRegistry from normalized checks/failures.
- (decision) [ASSUMPTION] All remote failures use a canonical envelope with category, retryability, scope, provider/service id, operation id, safe message, cause code, and optional retry-after; secrets and raw vendor payloads are excluded. Binds adapters, UI, retry policy, quarantine, and telemetry. Prevents vendor-specific branching and secret leakage.
- (decision) [ASSUMPTION] Specialist outputs are artifacts plus provenance, not free-form mutations of session state. Binds AI-for-Thai adapters and evidence handling. Prevents service-specific response shapes and hidden data movement. Rule: each call records service id/version, input artifact references or approved payload class, consent decision when required, cache key, output artifact references, timing, and normalized failure; artifacts are stored by ArtifactStore and referenced by immutable ids.
- (decision) [ADOPTED] Credentials remain machine-local and outside SQLite/session/event payloads. Binds configuration and persistence. Prevents secret exfiltration and database coupling. Rule: CredentialStore returns opaque secret material only to the invoking adapter at call time; persisted state stores provider/service id and secret-free fingerprint only.
- (decision) [ASSUMPTION] Filesystem and process effects cross OS-specific PlatformBoundary adapters and fail closed. Binds tools on macOS and Windows. Prevents lexical path checks, shell differences, or child-only termination from creating incompatible safety guarantees. Rule: canonicalize real paths including symlinks, enforce workspace roots, use argument-vector execution rather than interpolated shell text where possible, constrain environment/cwd/executable policy, terminate process trees, and emit durable approval/result/checkpoint events.
- (decision) [ASSUMPTION] Mutating operations are checkpoint-aware and idempotency-aware. Binds write_file, run_command, and future mutating tools. Prevents retries or restoration from duplicating side effects. Rule: every mutating operation has an OperationId, precondition snapshot, approval record, and result; checkpoint creation/rollback is owned by CheckpointRepository and invoked by application policy, never by UI or adapters.
- (decision) [ADOPTED] No silent provider or specialist fallback. Binds routing and availability. Prevents different epics from substituting services invisibly. Rule: unavailable/quarantined dependencies produce explicit typed events and user-visible recovery actions; a different dependency is used only after an explicit routing decision recorded in the prompt round.
- (version) Release 1 runtime seed from cli lockfile: Node.js >=22; TypeScript 5.9.3; Ink 5.2.1; React 18.3.1; better-sqlite3 11.10.0; Vitest 2.1.9. These are brownfield snapshot pins and code owns later dependency changes.
- (assumption) [ASSUMPTION] The existing Typhoon model id and endpoint are prototype configuration, not an architecture pin; release freeze must verify the live model, endpoint, compatibility, and adapter behavior before shipping.
- (direction) Structural seed keeps cli/ as the Release 1 product root and converges it around application/domain/ports/adapters/ui boundaries without requiring a wholesale rewrite before feature work.
- (direction) Operational envelope is local npm distribution on Node.js for Windows and macOS; no hosted request-path service is required for Release 1. Local persistence, logs, artifacts, configuration, and credentials are per OS user.
- (question) [ASSUMPTION BLOCKER] Confirm whether the canonical application event schema and Session aggregate ownership proposed here should bind all Release 1 epics.
- (question) Release model/endpoint/adapter freeze, exact OS enforcement matrix, artifact retention limits, cache policy, and telemetry retention require implementation evidence or product/security acceptance before release.
- (direction) Deferred below feature altitude: exact SQLite table/column layout, individual tool schemas, individual specialist request/response mapping, Ink component tree, concrete logging library, installer mechanics, and post-Release-1 roles for client/ and server/.
- (decision) [ADOPTED] A versioned Capability Registry is the sole authority for specialist-service visibility and invokability. Binds catalog, router, health, adapters, and UI. Prevents independent disagreement about which services may execute. Rule: reviewed manifest entries declare stable id, contract version, input/output classes, credential group, lifecycle status, and invokable flag; routing and execution reject anything not integrated and invokable.
- (decision) [ADOPTED] Specialist cache identity and freshness are shared architecture behavior. Binds routing, artifacts, evidence, and UI. Prevents adapters inventing incompatible reuse semantics. Rule: cache key includes source content hash and service-configuration fingerprint; reuse emits visible provenance; callers can force a fresh run without deleting prior evidence.
- (decision) [ADOPTED] Authority is a product state lattice, not one permission level. Binds PEP, session, approvals, consent, and restoration. Prevents Full Access, sensitive transfer consent, temporary approval, and durable boundary expansion from being conflated. Rule: Work Mode, Permission Profile, temporary operation approval, sensitive-transfer consent, and durable Boundary Expansion are evaluated independently; durable boundaries are persisted/audited, temporary authority expires, and restoration never recreates transient approvals.
- (decision) [ADOPTED] Health failures propagate by explicit scope and dependency graph. Binds provider and specialist health. Prevents request-local errors from quarantining configurations and shared-key failures from leaving dependents falsely available. Rule: normalized failures declare request, configuration, service, or credential-group scope; a rejected shared credential updates all dependent services, while transient request failures do not mutate durable health unless threshold policy says so.
- (decision) [ADOPTED] Rollback coverage is limited and honest. Binds checkpoints and mutating tools. Prevents claiming reversibility for shell or remote effects. Rule: automatic rollback covers checkpointed built-in file mutations only, verifies current content against the recorded post-image before reversal, stops and reports partial rollback on conflict, and never claims to reverse process, shell, or remote-service effects.
- (decision) [ADOPTED] Persisted evidence is crash-consistent through one local commit/recovery protocol. Binds session, artifacts, health, interruption, evidence, and checkpoints. Prevents orphaned artifacts and half-recorded operations being treated as complete. Rule: durable records carry operation ids and commit state; completion becomes visible only after all required references are durable; startup recovery detects and marks or repairs incomplete operations without replaying side effects.
- (constraint) Supported Release 1 environments are Windows 11 25H2+ with Windows Terminal and PowerShell, and macOS 14+ with Terminal and zsh; Node.js 22+ runtime with clean-machine validation on Node.js 24 LTS. Linux, WSL, and non-native Windows shells are out of scope.
- (assumption) [ASSUMPTION] The full Hexagonal domain/application/ports/adapters taxonomy is the convergence target, not an already adopted directory regime; only the headless CoreApp/Ink separation is adopted by current ADR and code.
- (decision) [ADOPTED] RuntimeActivation is separate from persisted Session authority. Binds restoration and permissions. Prevents restored sessions from reviving temporary approvals or elevated profiles. Rule: persist Work Mode and durable Boundary Expansions; each process activation starts Permission Profile at Manual, has no temporary approvals or transfer consent, and records any later authority changes as new events.
- (decision) [ADOPTED] Sensitive persisted session content uses authenticated encryption at rest. Binds SQLite persistence and workspace lookup. Prevents plaintext transcript leakage or database-only key compromise. Rule: AES-256-GCM encrypts sensitive fields with a per-install data-encryption key stored outside SQLite; only minimum metadata and keyed workspace equality index remain plaintext; decryption occurs inside the trusted local process.
- (decision) [ADOPTED] Capability discovery and execution preflight are local and registry-backed. Binds Plan mode, catalog, adapters, and routing. Prevents the model inventing capabilities, installer URLs, or invoking catalogued-only services. Rule: the reviewed manifest declares support level and trusted metadata; application preflight checks invokability, credential/health state, consent, input class, and mode before dispatch; unavailable capabilities return typed remedies.
- (constraint) Release 1 Phase 1 remote path calls Typhoon and AI-for-Thai directly from local adapters; any hosted Compatibility Layer/API is Phase 2 and cannot enter the Release 1 request path without an architecture update.
- (decision) [ADOPTED] Typhoon is the default and only launch reasoning provider; adapter/model/endpoint release pins remain a verification gate rather than allowing a different default.
- (question) [SOURCE CONFLICT] PRD commits Release 1 to Windows and macOS, while ADR 0008 records Windows-first with macOS deferred. Treat the newer PRD as the current product target unless the user confirms ADR 0008 still governs; platform scope remains a finalization blocker.
- (direction) Context projection is adopted by ADR but not yet wired in runtime; implementation must replace full-history dispatch and satisfy protected-content preflight and compaction thresholds before compliance can be claimed.
- (decision) [ADOPTED] The authoritative Release 1 PRD supersedes the older competition-prototype Windows-only scope: public Release 1 supports Windows 11 25H2+ and macOS 14+; ADR 0008 remains historical prototype context and requires later reconciliation.
- (decision) [ASSUMPTION] CoreApp uses a positive protocol: dispatch typed intents, subscribe to session events from an after-sequence cursor, and query versioned read projections. Binds UI/core integration. Prevents callback-only approvals, incompatible stream ownership, and UI-owned domain projections. Rule: approvals/consent/cancellation are correlated intents and events; durable events publish only post-commit; token deltas may be transient but final content is durable; session/status/context/artifact/restoration views come from CoreApp queries.
- (decision) [ASSUMPTION] The PEP is technically non-bypassable. Binds all effect ports. Prevents application or adapter code from executing an effect after advisory-only checks. Rule: only the PEP-owned EffectExecutor receives concrete effect adapters; each effect requires an unforgeable operation authorization bound to operation digest, authority revision, policy decision, and expiry; adapters reject absent or mismatched authorization.
- (decision) [ASSUMPTION] Authority grants are typed exact capabilities. Binds Boundary Expansions, operation approvals, and sensitive transfers. Prevents path substitution, broad consent reuse, and retry authority leakage. Rule: boundary grants bind stable resource identity, canonical path, platform/workspace identity, allowed actions, expiry and revocation; approvals bind operation/action digest; transfer consent binds content hash, verified recipient capability/version/endpoint, purpose, call count, expiry, and operation or prompt scope.
- (decision) [ASSUMPTION] Authority is versioned and linearized at effect start. Binds UI changes, cancellation, PEP, and platform effects. Prevents stale approvals or profile changes racing an irreversible action. Rule: each authority mutation increments RuntimeActivation revision; operation authorization records that revision; the PEP atomically revalidates current authority and cancellation immediately before handing the effect to the adapter.
- (decision) [ASSUMPTION] Filesystem containment uses stable resource identity, not path text alone. Binds workspace and boundary adapters. Prevents symlink, junction, mount, case, Unicode, or rename substitution between check and use. Rule: resolve/revalidate platform-native canonical identity at authorization and effect time, operate through stable handles where available, and deny when equivalent protection cannot be established.
- (decision) [ASSUMPTION] Command execution uses a canonical fail-closed policy. Binds macOS and Windows process adapters. Prevents PATH, inherited environment, shell startup hooks, and child-process behavior from changing authority. Rule: resolve approved executable identity, validate argument vector/cwd/environment, pass a minimal allowlisted environment, avoid user startup hooks/config, isolate process group/tree, enforce timeout/cancellation, and deny when the platform enforcement matrix cannot be satisfied.
- (decision) [ASSUMPTION] SessionRepository owns one append/replay event log. Binds persistence, CoreApp subscriptions, UI replay, and projections. Prevents per-operation and per-session sequence disagreement or duplicate rendering. Rule: allocate monotonic sequence and aggregate version atomically per SessionId; each event has immutable EventId and schema version; append is idempotent by EventId/OperationId; publish only after commit; replay is ordered and at-least-once so consumers deduplicate by EventId.
- (decision) [ASSUMPTION] Mutations use a canonical operation state machine including unknown outcome. Binds local effects, remote requests, checkpoints, retry, and recovery. Prevents duplicate shell/remote mutations after crashes. Rule: proposed, authorized, prepared, dispatched, succeeded, failed, cancelled, unknown-outcome, reconciled; an interrupted dispatched operation is never automatically retried unless adapter-specific idempotency and outcome reconciliation prove replay safe.
- (decision) [ASSUMPTION] Provider dispatch owns an immutable ContextManifest and authoritative transmitted-byte digest. Binds ContextBuilder and provider adapters. Prevents compaction and adapter serialization measuring different requests. Rule: the provider adapter finalizes canonical request serialization/tool schemas, measures against the verified effective context budget, returns the immutable manifest and digest before dispatch, and the session records exactly what was transmitted or stops for recompaction.
- (decision) [ASSUMPTION] Health, cache, and capability state use one immutable EffectiveConfigurationGeneration. Binds credentials, endpoint/mapping/adapter versions, health, cache, and UI. Prevents rotated keys or stale checks validating a different effective configuration. Rule: generation combines public configuration plus opaque credential revision and contract/adapter versions; HealthRegistry serializes versioned transitions and rejects stale results; user-visible capability state composes registry eligibility with generation health; cache lookup may return prior evidence while force-fresh requires current availability.
- (decision) [ADOPTED] Rollback retention is five subsequent prompts by default, configurable; per-checkpoint cap 100 MB and total cap 500 MB; binary originals are encrypted; over-cap mutation requires explicit confirmation without rollback protection; expiry removes originals and metadata.
- (decision) [ADOPTED] Release 1 capability floor includes workspace list/read/search, bounded create/edit/delete, controlled command execution, dependency preflight, Typhoon reasoning, and working T-OCR, Speech-to-Text, Extract Address, and Named Entity Recognition integrations. Other catalog entries are visible and non-invokable.
- (decision) [ASSUMPTION] Encryption key lifecycle is explicit. Binds encrypted persistence and recovery. Prevents silent data loss or accidental new-key overwrite. Rule: create one per-install DEK through OS credential storage, identify key version, support atomic rotation, enter locked recovery when the key is missing/unreadable, never generate a replacement over existing ciphertext, and disclose unrecoverability.
- (decision) [ASSUMPTION] RuntimeActivation identity is renewed on process start, session create/open/switch, and workspace rebind. Binds authority reset. Prevents Full Access or temporary grants leaking across session/workspace changes.
- (decision) [ADOPTED] Remote transport identity is non-bypassable. Binds provider and specialist adapters plus transfer consent. Prevents TLS downgrade, redirect substitution, and sending approved bytes to an unapproved recipient. Rule: require HTTPS with normal certificate and hostname verification, prohibit verification bypass/downgrade, reject cross-origin or unauthorized redirects, and revalidate the final origin against EffectiveConfigurationGeneration and TransferConsentGrant before sending material.
- (decision) [ASSUMPTION] Specialist cache identity is a versioned canonical manifest. Binds artifact resolver, specialist adapters, cache, and evidence. Prevents two epics hashing different semantic requests. Rule: CacheManifest includes schema version, capability/contract version, normalized semantic input manifest with ordered artifact ids and content hashes, effective configuration generation, consent-relevant transformation/redaction policy, and requested operation options; its canonical encoding digest is the cache key and is persisted with the result.
- (decision) [ASSUMPTION] Persistence migrations are store-wide and recoverable. Binds SQLite, artifact/checkpoint envelopes, journals, projections, and readers. Prevents mixed-version stores and interrupted upgrades being opened as healthy. Rule: one StoreFormatVersion gates all durable components; migration records intent/progress/checksum, is restartable or reversible, validates before promotion, and on incompatibility/failure opens read-only recovery without running effects or overwriting data.
- (decision) [ASSUMPTION] Encryption uses persisted versioned envelopes and journaled rotation across stores. Binds SQLite and encrypted artifact/checkpoint files. Prevents partial key rotation leaving mixed undecryptable data. Rule: each ciphertext envelope records algorithm, key id/version, nonce, authentication metadata, and format version; rotation stages a new key, journal-migrates and verifies every durable store, atomically promotes the active key only after completion, and remains recoverable with old key material until final cleanup.
- (decision) [ASSUMPTION] CoreApp protocol compatibility is fixed as a shared versioned contract. Binds UI and application epics. Prevents independently valid discriminated unions and projections from failing integration. Rule: CoreProtocolV1 owns exhaustive typed unions for prompt/session/authority/health/capability intents, Session/Status/Context/Artifact/Capability projections, durable lifecycle/evidence events, and transient TokenDelta/Progress events; shared schema fixtures are compatibility tests, unknown variants are rejected, and major-version mismatch fails startup.
- (decision) [ASSUMPTION] Effect dispatch has one linearization and terminal precedence rule. Binds PEP, cancellation/revocation, event log, and platform adapters. Prevents units disagreeing whether a cancelled or revoked effect occurred. Rule: before dispatch commit, cancellation/revocation denies and consumes no authority; PEP atomically consumes single-use authorization and appends EffectDispatchCommitted before invoking the native adapter; after that point authority changes affect future effects and the operation ends succeeded, failed, or unknown-outcome unless the adapter proves cancellation completed before any effect. Unknown outcome outranks optimistic cancellation and forbids automatic retry.
- (decision) [ADOPTED] One canonical Sanitizer is mandatory before any potentially sensitive content enters persistence, UI/display, logs/telemetry, export, model context, action preview, or payload summary. Binds every application output boundary. Prevents each epic applying incompatible redaction. Rule: sanitizer policies are versioned by content class and cover credentials/tokens, headers, URLs/query values, environment values, errors/stacks, command/tool output, remote payloads, paths and user content as classified; sanitized output carries policy version and source evidence reference, while raw export is disabled by default.
- (decision) [ADOPTED] ArtifactResolver owns type-specific containment, minimization, extraction, and transfer preparation. Binds workspace, context, specialist, consent, and artifact epics. Prevents remote adapters receiving unresolved paths or independently choosing what bytes leave the machine. Rule: resolve explicit in-workspace references to supported text/code/Markdown/image/audio/PDF/DOCX/bounded-directory types; validate stable containment/type/format/size/privacy/service compatibility; extract PDF/DOCX text locally by default; transfer originals only when required and exactly consented; unsupported binaries send sanctioned metadata only or fail; remote requests receive immutable prepared bytes/text plus manifest, never local paths as fetch authority.
- (decision) [ADOPTED] AES-256-GCM encryption requires unique nonces per key and authenticated record identity/schema. Binds every encrypted persistence adapter. Prevents nonce reuse and ciphertext being replayed under a different record meaning. Rule: nonce generation is collision-resistant and never repeats for one key; AAD binds store id, record/entity id, field/content class, schema/format version, and key version; decryption rejects any mismatch before data enters the application.
- (decision) [ASSUMPTION] Built-in file mutation uses effect-time compare-and-apply semantics. Binds checkpoints, filesystem adapter, and concurrent processes. Prevents stale preconditions overwriting user or parallel-agent changes. Rule: authorization records canonical resource identity and expected digest/version; PlatformBoundary revalidates and conditionally applies the change under a cross-process lock or equivalent atomic primitive; mismatch produces conflict without mutation; atomic replacement preserves the expected-resource identity policy.
- (decision) [ADOPTED] Release 1 invalid structured proposals are rejected after one local validation pass; no model repair request, provider retry, protocol reinterpretation, or policy relaxation occurs. This resolves the PRD FR-6 permissive wording in favor of the explicit Release 1 non-goal and reconciliation intent. The validation error is recorded and surfaced as the terminal outcome.
- (decision) [ASSUMPTION] Quota and configured resource boundaries use one atomic QuotaLedger owned by the PEP. Binds providers, specialist services, transfers, and cost/resource limits. Prevents concurrent operations passing the same remaining allowance. Rule: before dispatch, reserve allowance under OperationId and scope it to user budget, dependency/configuration generation, service, credential group, transfer class, or upstream quota as applicable; success commits actual consumption, known non-consumption releases it, and unknown outcome holds an indeterminate reservation until reconciliation; exhaustion produces a typed hard-boundary result.
- (decision) [ASSUMPTION] Session deletion and artifact/cache retention use one reference and tombstone lifecycle. Binds SessionRepository, ArtifactStore, cache, and privacy retention. Prevents deleting a session while silently retaining session-exclusive sensitive evidence or breaking artifacts shared by other sessions. Rule: deletion tombstones the session, atomically removes its references/cache links, decrements content-addressed reference counts, purges zero-reference sensitive artifacts/manifests and invalidates their cache entries, preserves bytes referenced by another live session or explicit retention record, and makes partial cleanup recoverable through the operation journal; exact time-based retention may remain policy-configurable.
- (decision) [ADOPTED] Transfer consent binds the exact prepared payload dispatched. Binds ArtifactResolver, PEP, and remote adapter. Prevents consent for one representation authorizing changed bytes. Rule: PreparedPayloadManifest has canonical digest plus exact payload-byte digest; TransferConsentGrant binds both with recipient identity/purpose; immediately before transport the PEP recomputes/verifies the manifest and byte digest, and any transformation or mismatch requires new consent.
- (decision) [ADOPTED] Received remote streaming output is durable before UI publication. Binds provider adapters, event log, transcript, and interruption recovery. Prevents a crash losing output already observed by the user. Rule: sanitized provider chunks carry upstream sequence/high-water identity and are idempotently appended as RemoteOutputObserved events before projection to UI; completion seals the stream; interruption restores durable chunks and appends ChatInterrupted at the reliable high-water mark without inventing or retrying the ending.
- (decision) [ADOPTED] Transient and quota failures cannot quarantine after one occurrence. Binds HealthRegistry policy. Prevents temporary upstream conditions becoming durable configuration invalidity. Rule: timeout, network loss, rate limit, quota exhaustion, upstream server failure, and unsupported input remain request/quota states after one occurrence; only deterministic authentication, protocol, or configuration evidence can immediately quarantine, while any transient threshold may affect availability/unhealthy state but never quarantine without deterministic invalidity.
- (decision) [ADOPTED] PEP owns a versioned PermissionMatrix mapping action class, Work Mode, Permission Profile, risk/sensitivity, and hard boundaries to allow, ask, or deny. Binds all tools and specialist transfers. Prevents epics interpreting Manual, Assisted, and Full Access differently. Rule: Manual asks for eligible mutations/sensitive actions and permits bounded reads; Assisted applies deterministic rules and asks on uncertainty; Full Access suppresses only eligible prompts inside declared boundaries; Plan denies mutation; sensitive transfer always uses separate consent; hard boundaries always deny.
- (decision) [ASSUMPTION] Active Model Context separates trusted instructions from instruction-inert untrusted data. Binds ContextBuilder, registry, tools, workspace evidence, remote output, and provider adapter. Prevents prompt injection in repository/artifact/tool/service content from becoming policy or command authority. Rule: only application-owned versioned instructions and registry tool schemas occupy trusted instruction channels; all user/workspace/artifact/tool/remote content is source-labelled, delimited, provenance-bound untrusted data; content cannot define tools, alter policy, grant authority, or supply executable registry metadata.
- (decision) [ASSUMPTION] Session deletion covers the full session-owned lifecycle, not only artifact references. Binds SessionRepository, event log, token ledger, context/evidence, checkpoints, projections, journal staging, and encrypted store cleanup. Prevents a deleted session remaining recoverable through secondary stores. Rule: tombstone and make transcript/events/context manifests/token ledger/checkpoints/projections inaccessible in one journaled operation, cascade artifact references under AD-26, checkpoint/clean WAL and staging remnants according to secure-retention policy, preserve only a non-sensitive deletion audit tombstone, and make partial deletion recoverable without reviving the session.
- (decision) [ASSUMPTION] Recursive directory deletion uses a frozen tree manifest and quarantine transition. Binds filesystem adapter and controlled deletion. Prevents descendant changes between approval and traversal from deleting unapproved content. Rule: approval binds canonical root identity plus ordered descendant identity/content manifest; effect-time revalidation must match; atomically rename/quarantine the root within the same filesystem before recursive removal when the platform can enforce it; otherwise deny recursive deletion or require a safer narrower operation; changes or open-handle uncertainty produce conflict/unknown outcome, never best-effort continued traversal.
- (decision) User adopted all remaining fast-path assumptions as binding Release 1 architecture decisions after the rubric, technology/reality, adversarial divergence, security, and data-integrity gates passed.
- (event) spine finalized
